{
  "name": "server",
  "version": "1.0.0",
  "main": "app.cjs",
  "scripts": {
    "dev": "nodemon app.cjs",
    "start": "node app.cjs",
    "render-build": "npm ci && cd ../client && npm ci && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "prisma": "^6.0.0"
  }
}