{
  "name": "server",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "start": "node app.cjs",
    "render:build": "echo 'server build step (none)'",
    "render:predeploy": "npx prisma migrate deploy && npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prisma": "^6.16.2"
  }
}